---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCompanyInfo } from "../lib/api";
const company = await getCompanyInfo("AGE");
const companyName = company?.legalName ?? "";
---

<BaseLayout
  title={`Page Not Found | ${companyName}`}
  description="The page you're looking for doesn't exist. Browse our fabric collections or get in touch with our team."
  noindex={true}
  waMessage="Hi, I couldn't find what I was looking for on your website. Can you help me?"
>
  <section class="relative overflow-hidden bg-[#f8f9fa] min-h-[calc(100vh-var(--nav-height,80px))] flex items-center pt-8 pb-10">

    <!-- Dot pattern background -->
    <div class="pointer-events-none absolute inset-0 opacity-[0.04]"
      style="background-image: radial-gradient(#041632 1px, transparent 0); background-size: 32px 32px;">
    </div>

    <!-- Skewed accent block (matches HeroSection) -->
    <div class="absolute top-0 right-0 w-1/3 h-full bg-[#041632]/5 -skew-x-12 transform translate-x-20 hidden lg:block pointer-events-none"></div>

    <!-- Giant faded 404 -->
    <div class="absolute inset-0 flex items-center justify-center pointer-events-none select-none">
      <span class="font-headline font-bold text-[20rem] md:text-[28rem] leading-none text-[#041632]/[0.04]">404</span>
    </div>

    <div class="relative z-10 mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 w-full">
      <div class="max-w-2xl">

        <span class="mb-3 inline-block rounded-full bg-[#fed488] px-4 py-1.5 text-xs font-label font-bold uppercase tracking-widest text-[#785a1a]">
          Error 404
        </span>

        <h1 class="mt-2 font-headline italic text-4xl md:text-6xl leading-[1.1] tracking-tight text-[#041632]">
          This thread<br/>
          doesn't lead<br/>
          <span class="text-[#775a19]">anywhere.</span>
        </h1>

        <p class="mt-4 text-base md:text-lg leading-relaxed text-[#44474d] max-w-lg">
          The page you're looking for has been moved, renamed, or never existed.
          Let's get you back on track.
        </p>

        <div class="mt-6 flex flex-wrap gap-3">
          <a
            href="/"
            class="inline-flex items-center gap-2 rounded-lg bg-[#041632] px-6 py-3 font-label font-bold text-sm uppercase tracking-widest text-white transition hover:bg-[#775a19] group"
          >
            Go to Homepage
            <span class="material-symbols-outlined text-lg transition-transform group-hover:translate-x-1">arrow_forward</span>
          </a>
          <a
            href="/fabric"
            class="inline-flex items-center gap-2 rounded-lg border border-[#c5c6ce] px-6 py-3 font-label font-bold text-sm uppercase tracking-widest text-[#041632] transition hover:bg-[#f3f4f5]"
          >
            Browse Collections
          </a>
          <a
            href="/support"
            class="inline-flex items-center gap-2 rounded-lg border border-[#c5c6ce] px-6 py-3 font-label font-bold text-sm uppercase tracking-widest text-[#041632] transition hover:bg-[#f3f4f5]"
          >
            Contact Us
          </a>
        </div>

        <!-- Quick links -->
        <div class="mt-8 pt-6 border-t border-[#c5c6ce]/40">
          <p class="font-label text-[10px] uppercase tracking-[0.25em] text-[#64748b] mb-3">Popular pages</p>
          <div class="flex flex-wrap gap-2">
            {[
              { label: "Fabric Catalog", href: "/fabric" },
              { label: "Capabilities", href: "/capabilities" },
              { label: "Certifications", href: "/certifications" },
              { label: "Blog", href: "/blog" },
              { label: "About", href: "/about" },
              { label: "FAQ", href: "/faq" },
              { label: "Careers", href: "/careers" },
            ].map((link) => (
              <a
                href={link.href}
                class="px-3 py-1.5 rounded-full border border-[#c5c6ce]/70 bg-white text-[#44474d] font-label text-[11px] font-bold uppercase tracking-wider hover:border-[#041632] hover:text-[#041632] hover:bg-[#041632]/5 transition-all"
              >
                {link.label}
              </a>
            ))}
          </div>
        </div>

      </div>
    </div>

  </section>
</BaseLayout>
