import { defineCollection } from "astro:content";

// No file-based collections — data is managed via src/data/*.ts files.
// This export satisfies Astro's content config requirement.
export const collections = {};
