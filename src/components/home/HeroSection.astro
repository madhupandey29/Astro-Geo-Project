---
import { getCompanyInfo, getTopicPageBySlug, buildWaLink, buildPhoneHref } from "../../lib/api";
import ResponsiveImage from "../common/ResponsiveImage.astro";

const [company, homePage] = await Promise.all([
  getCompanyInfo(),
  getTopicPageBySlug("home"),
]);
const heroImage = homePage?.image1CloudUrl ?? null;
const whatsappLink = buildWaLink(company?.whatsappNumber, "/support");
---

<section class="relative min-h-[600px] md:h-[870px] flex items-center bg-[#f3f4f5] overflow-hidden py-12 md:py-0">
  <div class="container mx-auto px-4 md:px-8 grid grid-cols-1 lg:grid-cols-12 gap-12 items-center relative z-10 w-full">
    <div class="lg:col-span-7">
      <p class="font-label text-[#775a19] font-bold tracking-[0.2em] md:tracking-[0.3em] uppercase mb-4 md:mb-6 text-xs md:text-sm text-center lg:text-left">
        Industrial Legacy
      </p>
      <h1 class="font-headline text-4xl sm:text-5xl md:text-8xl text-[#041632] leading-tight mb-6 md:mb-8 text-center lg:text-left">
        Premium Textile <span class="italic font-light">Manufacturing</span> Excellence since 1971
      </h1>
      <p class="font-body text-base md:text-xl text-[#44474d] max-w-xl mb-8 md:mb-10 leading-relaxed text-center lg:text-left mx-auto lg:mx-0">
        Supplying the world's leading fashion brands with ethically sourced, high-precision fabrics tailored for global scale.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
        <a
          href={whatsappLink}
          class="font-label bg-[#041632] text-white px-6 md:px-10 py-4 md:py-5 rounded-md font-bold tracking-widest uppercase hover:bg-[#775a19] transition-all shadow-xl flex items-center justify-center gap-3 text-xs md:text-sm"
        >
          Get a Wholesale Quote
          <span class="material-symbols-outlined text-sm">arrow_forward</span>
        </a>
        <a
          href="/fabric"
          class="font-label border border-[#c5c6ce] text-[#041632] px-6 md:px-10 py-4 md:py-5 rounded-md font-bold tracking-widest uppercase hover:bg-[#edeeef] transition-all text-xs md:text-sm text-center"
        >
          View Collections
        </a>
      </div>
    </div>

    <div class="lg:col-span-5 relative">
      <div class="aspect-[4/5] bg-[#e1e3e4] rounded-xl overflow-hidden shadow-2xl lg:rotate-2">
        <ResponsiveImage
          src={heroImage}
          alt={homePage?.altTextImage1 ?? "Premium textile fabric"}
          title={homePage?.altTextImage1 ?? "Premium textile fabric"}
          width={512}
          height={640}
          widths={[375, 480, 640, 768, 1024, 1280]}
          sizes="(min-width: 1280px) 540px, (min-width: 1024px) 42vw, (min-width: 768px) 60vw, 100vw"
          crop="fill"
          loading="eager"
          fetchpriority="high"
          class="w-full h-full object-cover"
        />
      </div>
      <div class="absolute -bottom-6 -left-2 md:-bottom-8 md:-left-8 bg-white p-4 md:p-8 rounded-xl shadow-xl max-w-[160px] md:max-w-xs border border-[#edeeef]">
        <p class="font-label text-[8px] md:text-xs text-[#775a19] font-bold mb-1 md:mb-2">DAILY OUTPUT</p>
        <p class="font-headline text-2xl md:text-4xl text-[#041632] font-bold">10,000+ <span class="text-sm md:text-lg">Meters</span></p>
      </div>
    </div>
  </div>

  <!-- Background Accent -->
  <div class="absolute top-0 right-0 w-1/3 h-full bg-[#041632]/5 -skew-x-12 transform translate-x-20 hidden lg:block"></div>
</section>
